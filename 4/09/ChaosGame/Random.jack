class Random {
	field int lastValue;

	constructor Random new(int seed) {
		let lastValue = seed;
		return this;
	}

	method int nextInt() {
		var int nextInt;
		let nextInt = Util.mod((lastValue * 8121) + 28411, 12345);
		let lastValue = nextInt;
		return nextInt;
	}
}